import { createTheme } from '@mui/material'

export default createTheme({
  palette: {
    primary: { main: '#d22a33' },
    secondary: { main: '#002365' },
    error: {
      main: '#f44336'
    }
  }
})
