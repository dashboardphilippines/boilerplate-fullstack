import { createTheme } from '@mui/material/styles'

export default createTheme({
  palette: {
    background: {
      paper: '#212121',
      default: '#212121'
    },
    primary: { main: '#d22a33' },
    secondary: { main: '#002365' }
  }
})
